import { DarkModeBody, DarkModeContainer, DarkModeTitle, DarkModeText, LightModeBody, LightModeContainer, LightModeTitle, LightModeText } from "./variaveis";

export const lightMode = {
    body: LightModeBody,
    container: LightModeContainer,
    title: LightModeTitle,
    text: LightModeText,
    filter:"",
}

export const darkMode = {
    body: DarkModeBody,
    container: DarkModeContainer,
    title: DarkModeTitle,
    text: DarkModeText,
    filter:"invert(100%)",
}